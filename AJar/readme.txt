dx --dex --output=ajar.jar dynamic.jar

升级思路：
1、首先是要集成ajar
2、使用的时候，先在本地目录下查找（目录下应包含jar和版本描述文件）
3、如果发现版本描述问价的版本号比当前的高，那么则采用classloader的方式调用接口
4、为了避免程序正在使用jar的时候更新的文件，应该在load前先讲文件复制到cache目录，然后再load。每次
调用前，都应先判断jar是否更新，如果更新，则重新加载。